<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Davish.Sendr.Notification</PackageId>
    <AssemblyName>Davish.Sendr.Notification</AssemblyName>
    <Version>3.0.0</Version>
    <Description>DEPRECATED: notification publishing (IPublisher, AddSendrNotification, AddNotificationHandler, ...) has moved into Davish.Sendr itself. This package is kept only so existing references keep resolving via its dependency on Davish.Sendr; it has no content of its own. Reference Davish.Sendr directly instead — this package will be removed in a future major version.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Sendr\Sendr.csproj" />
  </ItemGroup>

</Project>
